<?xml version="1.0"?>
<ruleset name="CMC">
  <description>Custom coding standards for CMC module.</description>

  <!-- Include our custom sniffs -->
  <rule ref="Drupal\cmc\Sniffs\Twig\DirectFieldAccessSniff"/>

  <!-- Define file extensions to check -->
  <arg name="extensions" value="html.twig"/>
</ruleset> 